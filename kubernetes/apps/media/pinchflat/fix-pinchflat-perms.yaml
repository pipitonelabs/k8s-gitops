apiVersion: v1
kind: Pod
metadata:
  name: fix-pinchflat-perms
  namespace: media
spec:
  containers:
  - name: fix
    image: busybox:latest
    command: ["sh", "-c", "chown -R 99:100 /config && chmod -R 775 /config && sleep 3600"]
    securityContext:
      runAsUser: 0
    volumeMounts:
    - name: config
      mountPath: /config
  volumes:
  - name: config
    persistentVolumeClaim:
      claimName: pinchflat