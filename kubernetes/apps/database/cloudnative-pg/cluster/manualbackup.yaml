---
apiVersion: postgresql.cnpg.io/v1
kind: Backup
metadata:
  name: manual-full-backup-$(date +%s)  # Unique name with timestamp
spec:
  # Optional: Override defaults from your Cluster spec
  # method: barmanObjectStore  # Or 'plugin' if using CNPG-I
  # target: primary  # Or 'prefer-standby' to use a replica
  cluster:
    name: postgres17